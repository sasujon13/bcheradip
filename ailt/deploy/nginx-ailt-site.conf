# /ailt is handled by Angular (fcheradip) — route path: 'ailt'
# Do NOT add a static alias for /ailt/ — it breaks https://cheradip.com/ailt
#
# Keep only the API proxy (ailt_api/deploy/nginx-ailt-api.conf):
#
# location /ailt/api/ {
#     proxy_pass http://127.0.0.1:8790/api/ailt/;
#     ...
# }

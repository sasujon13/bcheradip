# If your server uses Apache (not nginx) for cheradip.com, enable proxy modules:
#   sudo a2enmod proxy proxy_http
#   sudo cp ailt_api/deploy/apache-ailt-api.conf /etc/apache2/conf-available/ailt-api.conf
#   sudo a2enconf ailt-api
#   sudo systemctl reload apache2
#
# Place INSIDE the VirtualHost that serves /var/www/cheradip (or cheradip.com).

    ProxyPreserveHost On
    ProxyPass /ailt/api/ http://127.0.0.1:8790/api/ailt/
    ProxyPassReverse /ailt/api/ http://127.0.0.1:8790/api/ailt/
